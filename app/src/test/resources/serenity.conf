environment = "test"

webdriver {
  driver = "chrome"
  base {
    url = "https://bikeleasing.de"
  }
  headless = true
}


drivers {
  chrome {
    options = ["--disable-web-security", "--start-maximized"]

  }
}

api {
  base {
    url = "https://catfact.ninja"
  }
  facts{
   uri = "/facts?page=1"
  }
}

serenity {
  take.screenshots = FOR_FAILURES
  project.name = "Serenity Kotlin Framework"
  environment = test
}